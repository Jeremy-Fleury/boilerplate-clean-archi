model Todo {
    uuid         String   @id
    title        String
    description  String
    isDone       Boolean
    createdAt    DateTime @default(now())
    updatedAt    DateTime @updatedAt
    todoList     TodoList @relation(fields: [todoListUuid], references: [uuid])
    todoListUuid String
}
